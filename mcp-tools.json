[
  {
    "name": "start_recording",
    "description": "Start a screen recording (Windows gdigrab) as a background ffmpeg process. Returns a session id and output path. Recording is explicit and never auto-starts. Audio is not captured in Phase 1 (video only). A window: target records the on-screen rectangle the window occupies, resolved once at start (window must be visible, on top, not minimized); it does not follow a window moved/resized mid-recording.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "target": { "type": "string", "description": "full | monitor:<index> (0 = primary) | window:<title> | region:<x>,<y>,<w>,<h>. window: captures the rectangle the window occupies as displayed (case-insensitive exact title, else substring; topmost match)" },
        "fps": { "type": "integer", "minimum": 1, "maximum": 120, "description": "Frames per second (default 30)" },
        "quality": { "type": "string", "enum": ["draft", "standard", "high"], "description": "Encoder preset (default standard)" },
        "output": { "type": "string", "description": "Optional output .mp4 path (defaults under SCREENCAST_HOME/recordings)" }
      },
      "required": ["target"]
    }
  },
  {
    "name": "stop_recording",
    "description": "Stop a recording by session id. Sends ffmpeg a graceful quit so the file is finalized rather than truncated, then returns the final path and duration.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "sessionId": { "type": "string", "description": "Session id returned by start_recording" }
      },
      "required": ["sessionId"]
    }
  },
  {
    "name": "list_sessions",
    "description": "List all recording sessions (active and finished) with their status, target, output path, and whether the process is still alive.",
    "inputSchema": { "type": "object", "properties": {}, "required": [] }
  },
  {
    "name": "get_session",
    "description": "Inspect a single recording session by id, including live status and whether its output file exists on disk.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "sessionId": { "type": "string", "description": "Session id returned by start_recording" }
      },
      "required": ["sessionId"]
    }
  },
  {
    "name": "screenshot",
    "description": "Capture a single still frame (PNG) of a target (full screen, a monitor, a window, or a region). Returns the image path. A window: target captures the on-screen rectangle the window occupies as currently displayed (it must be visible, on top, and not minimized).",
    "inputSchema": {
      "type": "object",
      "properties": {
        "target": { "type": "string", "description": "full | monitor:<index> | window:<title> | region:<x>,<y>,<w>,<h>. window: captures the rectangle the window occupies as displayed (case-insensitive exact title, else substring; topmost match)" },
        "output": { "type": "string", "description": "Optional output .png path (defaults under SCREENCAST_HOME/screenshots)" }
      },
      "required": ["target"]
    }
  },
  {
    "name": "sample_frames",
    "description": "Sample frames from a video so the agent can view what happened: either at a fixed rate (fps) or at explicit timestamps. Writes PNG frames to a folder and returns their paths.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "Path to the source video file" },
        "fps": { "type": "number", "description": "Sample this many frames per second (mutually exclusive with timestamps)" },
        "timestamps": { "type": "array", "items": { "type": "number" }, "description": "Explicit second offsets to grab one frame at each (mutually exclusive with fps)" },
        "outputDir": { "type": "string", "description": "Optional folder for the frames (defaults under SCREENCAST_HOME/frames)" }
      },
      "required": ["input"]
    }
  },
  {
    "name": "get_media_info",
    "description": "Probe a media file with ffprobe and return duration, resolution, frame rate, codecs, container format, and size.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "Path to the media file to probe" }
      },
      "required": ["input"]
    }
  },
  {
    "name": "trim",
    "description": "Trim a video to a sub-clip by start + (end or duration). Uses stream copy for speed; cut points snap to the nearest keyframe.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "Path to the source video" },
        "start": { "type": "number", "description": "Start time in seconds" },
        "end": { "type": "number", "description": "End time in seconds (use end OR duration)" },
        "duration": { "type": "number", "description": "Clip length in seconds (use end OR duration)" },
        "output": { "type": "string", "description": "Optional output path (defaults under SCREENCAST_HOME/edits)" }
      },
      "required": ["input", "start"]
    }
  },
  {
    "name": "concat",
    "description": "Concatenate two or more videos into a single file using the ffmpeg concat demuxer (stream copy). Inputs should share the same codec and format.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "inputs": { "type": "array", "items": { "type": "string" }, "minItems": 2, "description": "Two or more video paths to join, in order" },
        "output": { "type": "string", "description": "Optional output path (defaults under SCREENCAST_HOME/edits)" }
      },
      "required": ["inputs"]
    }
  },
  {
    "name": "convert",
    "description": "Convert a video between mp4, gif, and webm. mp4 uses H.264, webm uses VP9, and gif uses a palette filter for quality. Returns the output path.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "Path to the source media" },
        "format": { "type": "string", "enum": ["mp4", "gif", "webm"], "description": "Target format" },
        "fps": { "type": "number", "description": "Output fps (gif only; default 12)" },
        "width": { "type": "number", "description": "Output width in px, height auto (gif only; default 720)" },
        "output": { "type": "string", "description": "Optional output path (defaults under SCREENCAST_HOME/edits)" }
      },
      "required": ["input", "format"]
    }
  },
  {
    "name": "crop",
    "description": "Crop a video to a pixel rectangle (x, y, width, height) and re-encode. A rectangle that runs off the source frame is rejected rather than silently clamped.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "Path to the source video" },
        "x": { "type": "integer", "minimum": 0, "description": "Left edge of the crop in pixels" },
        "y": { "type": "integer", "minimum": 0, "description": "Top edge of the crop in pixels" },
        "width": { "type": "integer", "minimum": 1, "description": "Crop width in pixels" },
        "height": { "type": "integer", "minimum": 1, "description": "Crop height in pixels" },
        "output": { "type": "string", "description": "Optional output path (defaults under SCREENCAST_HOME/edits)" }
      },
      "required": ["input", "x", "y", "width", "height"]
    }
  },
  {
    "name": "scale",
    "description": "Resize a video to a target width and/or height and re-encode. Provide one side to keep the aspect ratio, or both for an exact size.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "Path to the source video" },
        "width": { "type": "integer", "minimum": 1, "description": "Target width in px (omit to derive from height)" },
        "height": { "type": "integer", "minimum": 1, "description": "Target height in px (omit to derive from width)" },
        "output": { "type": "string", "description": "Optional output path (defaults under SCREENCAST_HOME/edits)" }
      },
      "required": ["input"]
    }
  },
  {
    "name": "speed",
    "description": "Change playback speed by a factor (>1 faster, <1 slower) and re-encode. Audio is retempo'd to match when the source has an audio track.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "Path to the source video" },
        "factor": { "type": "number", "exclusiveMinimum": 0, "description": "Speed multiplier: >1 faster, <1 slower (2 = double speed)" },
        "output": { "type": "string", "description": "Optional output path (defaults under SCREENCAST_HOME/edits)" }
      },
      "required": ["input", "factor"]
    }
  },
  {
    "name": "overlay",
    "description": "Composite an image or video (logo, watermark, picture-in-picture) onto a base video at a pixel position, optionally scaled and time-limited.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "Path to the base video" },
        "overlay": { "type": "string", "description": "Path to the overlay image or video" },
        "x": { "type": "integer", "minimum": 0, "description": "Overlay left position in pixels" },
        "y": { "type": "integer", "minimum": 0, "description": "Overlay top position in pixels" },
        "width": { "type": "integer", "minimum": 1, "description": "Optional overlay width in px (height auto)" },
        "height": { "type": "integer", "minimum": 1, "description": "Optional overlay height in px (width auto)" },
        "start": { "type": "number", "minimum": 0, "description": "Optional second to show the overlay from" },
        "end": { "type": "number", "exclusiveMinimum": 0, "description": "Optional second to hide the overlay after" },
        "output": { "type": "string", "description": "Optional output path (defaults under SCREENCAST_HOME/edits)" }
      },
      "required": ["input", "overlay", "x", "y"]
    }
  },
  {
    "name": "compress",
    "description": "Re-encode a video to a smaller file with a CRF quality ladder (light/medium/heavy) and an optional width cap that only downscales.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "Path to the source video" },
        "level": { "type": "string", "enum": ["light", "medium", "heavy"], "description": "Compression strength (CRF 23 / 28 / 32; default medium)" },
        "maxWidth": { "type": "integer", "minimum": 1, "description": "Optional width cap in px; only ever downscales" },
        "output": { "type": "string", "description": "Optional output path (defaults under SCREENCAST_HOME/edits)" }
      },
      "required": ["input"]
    }
  },
  {
    "name": "extract_audio",
    "description": "Extract the audio track of a media file to its own file (mp3, aac, wav, or copy). Returns the output path.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "Path to the source media" },
        "format": { "type": "string", "enum": ["mp3", "aac", "wav", "copy"], "description": "Audio output codec; copy keeps the source codec without re-encoding" },
        "output": { "type": "string", "description": "Optional output path (defaults under SCREENCAST_HOME/edits)" }
      },
      "required": ["input", "format"]
    }
  },
  {
    "name": "clip",
    "description": "Extract one or more frame-accurate sub-segments to separate files. Unlike trim (fast stream copy that snaps to a keyframe), clip re-encodes so cuts land exactly on the given times.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "input": { "type": "string", "description": "Path to the source video" },
        "segments": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "properties": {
              "start": { "type": "number", "minimum": 0, "description": "Segment start in seconds" },
              "end": { "type": "number", "exclusiveMinimum": 0, "description": "Segment end in seconds" }
            },
            "required": ["start", "end"]
          },
          "description": "One or more {start, end} segments; each becomes its own output file"
        },
        "output": { "type": "string", "description": "Optional output path (single segment only; multi-segment lands under SCREENCAST_HOME/edits)" }
      },
      "required": ["input", "segments"]
    }
  }
]
